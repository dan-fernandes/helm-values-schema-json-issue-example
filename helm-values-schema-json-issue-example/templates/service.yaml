apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-values-schema-json-issue-example.fullname" . }}
  labels:
    {{- include "helm-values-schema-json-issue-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-values-schema-json-issue-example.selectorLabels" . | nindent 4 }}
