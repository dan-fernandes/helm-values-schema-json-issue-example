apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-values-schema-json-issue-example.fullname" . }}-test-connection"
  labels:
    {{- include "helm-values-schema-json-issue-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-values-schema-json-issue-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
